import {Category} from "@commerce/types/site";
import {Magento2Config} from "@framework/api";

const getVendors = async (config: Magento2Config): Promise<Category[]> => {
  return []
}

export default getVendors
